import React from "react";

/**
 * Independent screen dedicated to show alert notification message that block the other current activities managed by the user (e.g blocking in background the existing opened act panel screens).
 * @returns {JSX.Element}
 * @constructor
 */
export default function AlertScreenDisplay() {

    return (
        <>
            <h1>ALERT SCREEN DISPLAY</h1>
            <p>use of ReactBoostrap component named Modal or Toast</p>
        </>
    );

};
