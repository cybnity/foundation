apiVersion: v1
kind: Service
metadata:
  name: {{ include "ui-apis-gateway.fullname" . }}
  labels:
    {{- include "ui-apis-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ui-apis-gateway.selectorLabels" . | nindent 4 }}
