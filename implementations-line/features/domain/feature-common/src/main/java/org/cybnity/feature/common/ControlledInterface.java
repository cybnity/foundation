package org.cybnity.feature.common;

/**
 * Capability allowed to make interaction with one or several resource types
 * (e.g via a remote call on a protocol, via specific data structure exchange).
 * Can be considered as a mean (e.g Application Public Interface (API)) to apply
 * changes on a system, an application, an information or any type of resource
 * authorized and controlled at the runtime (e.g via access control, permissions
 * check...).
 */
public interface ControlledInterface {

}
