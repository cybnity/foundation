package org.cybnity.feature.common;

/**
 * CYBNITY domain function resource equals to a security capability provided by
 * an Application (bounded context). Also named "Capability" when function is
 * provided via a User Interface (UI).
 */
public interface Feature extends ResourceType {

}
